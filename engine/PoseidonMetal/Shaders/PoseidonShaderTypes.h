#ifndef POSEIDON_METAL_SHADER_TYPES_H
#define POSEIDON_METAL_SHADER_TYPES_H

// M0 has no shader constants. This shared header is intentionally present so
// the runtime-shader path exercises the same pre-expansion mechanism M1 uses.

#endif
